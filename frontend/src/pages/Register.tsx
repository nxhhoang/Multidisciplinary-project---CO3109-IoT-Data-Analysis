import React from 'react';
import { Register as RegisterFeature } from '../features/auth/Register';

export const RegisterPage: React.FC = () => {
  return <RegisterFeature />;
};

export default RegisterPage;
