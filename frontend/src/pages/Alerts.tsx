import React from 'react';
import { Alerts as AlertsFeature } from '../features/alerts/Alerts';

export const AlertsPage: React.FC = () => {
  return <AlertsFeature />;
};

export default AlertsPage;
